services:
  backend:
    build: .
    container_name: trade-surveillance-backend
    env_file:
      - .env
    command: uvicorn trade_surveillance.api.main:app --host 0.0.0.0 --port 8000 --reload
    ports:
      - "8000:8000"
